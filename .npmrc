# pnpm configuration for SchedAssist
# node-linker=hoisted is required for WSL environments where
# hardlinks/symlinks to a Windows-mounted filesystem (/mnt/) fail
# with EACCES/EXDEV errors.
node-linker=hoisted
